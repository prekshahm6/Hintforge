[project]
name = "hack"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "flockbird19", email = "vendev19@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "openai>=1.0.0",
    "uvicorn>=0.30.0",
    "pydantic>=2.7.0",
    "pypdf>=5.0.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
hack = "hack:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
